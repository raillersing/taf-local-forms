# TAf Local Forms v0.2.0

## Résumé

Modules 2, 3 et 4 disponibles dans l'application locale terrain.

### Nouvelles fonctionnalités

- **F020 — Page d'accueil avec choix étudiant / formateur** : la racine `/` affiche deux cartes de choix : « Je suis étudiant » (→ `/modules/`) et « Je suis formateur » (→ `/dashboard/`)
- **F020 — Espace modules étudiant** (`/modules/`) : nouvelle page qui liste les modules avec statut (Réponses ouvertes / Consultation / Indisponible), un bloc pédagogique décrivant les objectifs de formation, et un bouton d'accès au questionnaire
- **F020 — Blocs pédagogiques par module** : présentations intégrées (`module_X_pedagogy.html`) détaillant les objectifs, le contenu et le déroulement de chaque module
- **F021 — Contenu réel des présentations PowerPoint intégré dans l'espace étudiant** :
  - "Contenu réel des présentations PowerPoint intégré dans l'espace étudiant"
  - "Module 2 : Comprendre Internet — notions clés (navigateur, moteur de recherche), méthode en 4 étapes, activités pratiques"
  - "Module 3 : Recherche efficace — formule des bons mots-clés (matière + notion + niveau + type), 4 étapes question→mots-clés→résultats→choix, outils de recherche, erreurs fréquentes"
  - "Module 4 : Sources fiables — méthode des 5 questions (QUI ? QUOI ? QUAND ? OÙ ? POURQUOI ?), indices auteur/date/preuves, réflexes à retenir"
  - "Nouvelles classes CSS : pedagogy-card, pedagogy-grid, pedagogy-pill, pedagogy-steps, pedagogy-warning, pedagogy-formula, pedagogy-tip, pedagogy-retention"
  - "176 tests (10 nouveaux tests de contenu pédagogique)"
- **Module 2** — Comprendre Internet (questionnaire complet)
- **Module 3** — Recherche efficace (questionnaire complet avec quiz à 7 questions)
- **Module 4** — Sources fiables (questionnaire complet avec auto-évaluation, todo, quiz, activité pratique)
- **Module 5** — Email et outils de communication (questionnaire complet avec auto-évaluation, 8 todo, quiz 7 questions, exercice rédaction email, score max 7)
- **Accueil public** (`/`) — liste des modules actifs
- **Cockpit formateur** (`/dashboard/`) — centralise tous les outils
- **Dashboards par module** (`/dashboard/module-2/`, `/module-3/`, `/module-4/`, `/module-5/`) — statistiques, filtres, tableau des réponses
- **Dashboard Module 4** — inclut un résumé des décisions (fiable / douteuse / pas encore)
- **Exports CSV** par module (`/dashboard/export/module-X.csv`)
- **Page accès réseau** (`/dashboard/network/`) — diagnostic IP, toutes les URLs M2/M3/M4/M5, port, alertes
- **Configuration réseau sécurisée** (`/dashboard/settings/`) — modification de TAF_HOST_PORT, TAF_LAN_HOST, ALLOWED_HOSTS, CSRF_TRUSTED_ORIGINS, TIME_ZONE depuis l'interface (staff only, secrets jamais exposés)
- **Compteur temps réel** — présence élèves en cours de saisie affiché dans le cockpit formateur (heartbeat HTTP 15s étudiant, polling 8s dashboard)
- **Manuel terrain** (`docs/FICHE_DEMARRAGE_TERRAIN.md`) — pas à pas pour le formateur
- **Workflow agents et skills** — framework Codex orchestrateur
- **Graphify** — visualisation d'architecture (optionnel, nécessite Graphviz)
- **Nettoyage sûr** — scripts `taf-clean-status`, `taf-clean-after-merge`, `taf-docker-safe-clean`
- **Admin personnalisé** — `site_header`, `site_title`, `index_title`, `site_url` personnalisés ; template `base_site.html` avec barre de navigation vers cockpit/réseau/configuration ; CSS local sans dépendance externe
- **Tableaux Admin améliorés** — `list_display`, `search_fields`, `list_filter`, `readonly_fields` pour tous les modèles y compris `FormPresence`
- **Diagnostic LAN** — script shell `scripts/dev/taf-lan-diagnose`, scripts PowerShell `scripts/windows/taf-lan-open-port.ps1` et `taf-lan-show-status.ps1`
- **Page réseau enrichie** — section Diagnostic LAN avec indicateur WSL, passerelle, référence aux scripts
- **Guide de dépannage** — `docs/network/WINDOWS_WSL_LAN_TROUBLESHOOTING.md` complet
- **Dashboard UX repensé** — sous-navigation par onglets (Vue d'ensemble / Modules / Présence / Réseau / Exports / Admin)
- **Navigation améliorée** — le logo redirige vers le cockpit, état actif dans la barre de navigation, liens rapides vers réseau/configuration/admin
- **F022R — Rewiring global de navigation (215 tests)** : séparation complète des contextes étudiant / formateur / public ; logo → `/` partout ; nav contextuelle (Accueil + Modules étudiant ; Accueil + Cockpit + Réseau + Configuration + Admin formateur) ; pages détail étudiant par module (`/modules/module-X/`) ; CTA visibles (`.cta-student`, `.cta-consult`) ; pédagogie déplacée de la liste vers les pages détail ; alignement CSS nav en flexbox ; `<IP_DU_LAPTOP>` remplacé par message clair si `TAF_LAN_HOST` non configuré
- **Adresse IP locale affichée** — bannière verte dans le dashboard quand `TAF_LAN_HOST` est configuré
- **Liens réseau en target_blank** — tous les liens réseau (modules, dashboard, CSV, admin) s'ouvrent dans un nouvel onglet avec `rel="noopener noreferrer"`
- **Cartes modules enrichies** — affichage du statut, description, switch d'ouverture/fermeture des réponses par module
- **Admin CSS contrast amélioré** — thème visuel cohérent avec le dashboard (navbar, breadcrumbs, calendars, filters)
- **F025 — Module 5 – Email et outils de communication intégré (298 tests)** : `/modules/module-5/`, `/module-5/`, `/dashboard/module-5/`, export CSV, `seed_module5`, score max 7, 8 todo, exercice rédaction email, présence temps réel
- **F023 — Synchronisation automatique de l'IP LAN (255 tests)** : détection de l'IPv4 privée depuis la requête (priorité 1), depuis `TAF_LAN_HOST` (priorité 2), depuis détection automatique (priorité 3), ou statut non configuré ; source `request`/`settings`/`candidate`/`missing` ; drapeau `lan_host_stale` si l'IP configurée diffère de l'IP actuelle ; action « Utiliser l'adresse actuelle » dans `/dashboard/settings/` qui met à jour `TAF_LAN_HOST`, `TAF_HOST_PORT`, `ALLOWED_HOSTS` et `CSRF_TRUSTED_ORIGINS` en un clic ; nouveau `recommended_student_base_url` affiché dans le dashboard et la page réseau ; port LAN `8011` documenté ; script Windows `taf-lan-sync.ps1` (portproxy 8011→8010, pare-feu, sync Django) ; `taf-lan-install-auto-sync.ps1` (tâche planifiée au logon + répétition) ; `taf-lan-show-status.ps1` amélioré ; management command `sync_lan_settings` ; 40 nouveaux tests F023
- **F028 — Module 8 – Synthèse et exercices pratiques intégré** : `/modules/module-8/`, `/module-8/`, `/dashboard/module-8/`, export CSV 40 colonnes, `seed_module8`, score max 7 (6 V/F + 1 exact-match Q7), 10 todo (méthode de recherche en 6 étapes), 4 feedback, bloc pédagogique complet (7 sections), présence temps réel, 46 nouveaux tests F028
- **F033 — Refonte UI/UX globale** : design system CSS local (`static/css/app.css`), layout unifié (`templates/base.html`) avec skip-link et hero par page, navigation contextuelle étudiant/formateur, cockpit formateur enrichi (Modules 2–8, breadcrumbs, cards, badges), espace étudiant `/modules/` et pages détail refondues, assistant LAN 7 étapes avec statut global, page Sauvegarde/Diagnostic `/dashboard/backup/`, accessibilité (skip-link, ARIA, contrastes), pas de CDN, pas de secrets dans les templates, 15 tests UI RedesignUITests

## Validation

| Gate | Statut |
|------|--------|
| `manage.py check` | ✅ 0 issues |
| `manage.py test surveys.tests` | ✅ (15 nouveaux tests F033 + tests existants) |
| `manage.py makemigrations --check --dry-run` | ✅ Aucune migration en attente |
| `docker compose config` | ✅ Valide |
| `curl /` → 200 | ✅ |
| `curl /module-2/` → 200 | ✅ |
| `curl /module-3/` → 200 | ✅ |
| `curl /module-4/` → 200 | ✅ |
| `curl /module-5/` → 200 | ✅ |
| `curl /module-6/` → 200 | ✅ |
| `curl /module-7/` → 200 | ✅ |
| `curl /module-8/` → 200 | ✅ |
| Dashboards → 302 (non connecté) | ✅ |
| `/dashboard/settings/` → 302 (non connecté) | ✅ |
| `/dashboard/backup/` → 302 (non connecté) | ✅ |
| `/dashboard/presence.json` → 302 (non connecté) | ✅ |
| Aucun secret dans le diff | ✅ |
| Aucune migration non appliquée | ✅ |
| `git diff --check` | ✅ Pas d'erreur d'espace |

## Limites connues

- L'accès depuis un téléphone dépend de la qualité du Wi-Fi, du pare-feu Windows et de l'absence d'AP isolation.
- `TAF_LAN_HOST` doit être configuré dans `.env` pour figer l'adresse recommandée si l'IP change entre les sessions.
- Graphviz est optionnel pour générer les SVG d'architecture.
- Le port exposé par défaut est `8010` (modifiable via `TAF_HOST_PORT`).
- Le compteur de présence temps réel est indicatif : un élève qui ferme brutalement le navigateur disparaît après ~60s.
- Les changements réseau depuis `/dashboard/settings/` nécessitent un redémarrage Docker pour prise d'effet.
- Le panneau de configuration est réservé aux utilisateurs staff/superuser.

## Commandes de release

```bash
git tag -a v0.2.0 -m "v0.2.0 — modules 2 3 4 field release"
git push origin v0.2.0
gh release create v0.2.0 \
  --title "TAf Local Forms v0.2.0" \
  --notes-file docs/releases/v0.2.0.md \
  --verify-tag
```
